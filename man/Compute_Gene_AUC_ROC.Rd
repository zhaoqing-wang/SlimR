% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Compute_Gene_AUC_ROC.R
\name{Compute_Gene_AUC_ROC}
\alias{Compute_Gene_AUC_ROC}
\title{Compute AUC and Optionally Plot ROC Curve for a Single Gene}
\usage{
Compute_Gene_AUC_ROC(
  seurat_obj,
  gene,
  group_col,
  group_label,
  assay = "RNA",
  method = c("raw", "rank"),
  min_expression = NULL,
  plot = TRUE,
  plot_title = "ROC Curve",
  line_color = "firebrick",
  line_size = 1
)
}
\arguments{
\item{seurat_obj}{A Seurat object containing single-cell expression data.}

\item{gene}{A single character string specifying the gene to evaluate. Must
be present in the rownames of the chosen assay.}

\item{group_col}{Character string giving the name of a column in
\code{seurat_obj@meta.data} that defines cell groups (e.g., \code{"seurat_clusters"}).}

\item{group_label}{The value in \code{group_col} that defines the positive class
(e.g., \code{1} for cluster 1). All other cells are treated as negatives.}

\item{assay}{Character string specifying which assay to use. Default is
\code{"RNA"}.}

\item{method}{Scoring method: \code{"raw"} uses (possibly truncated)
expression values; \code{"rank"} uses expression ranks, which is robust
to dropout and does not require a \code{min_expression} threshold.}

\item{min_expression}{Numeric threshold for expression truncation (only
used when \code{method = "raw"}). Values below this threshold are set to
zero. Default is \code{NULL} (no truncation).}

\item{plot}{Logical indicating whether to create and return a ggplot2 ROC
curve. Default is \code{TRUE}.}

\item{plot_title}{Character string used as the title of the ROC plot.
Default is \code{"ROC Curve"}.}

\item{line_color}{Colour of the ROC curve. Default is \code{"firebrick"}.}

\item{line_size}{Numeric value for the thickness of the ROC curve line.
Default is \code{1}.}
}
\value{
A list with elements: \code{AUC}, \code{roc_data}, \code{predictions},
\code{labels}, and \code{roc_plot} (if \code{plot = TRUE}).
}
\description{
Evaluates the discriminatory power of a single gene in separating a
user-defined positive cell group from the rest, using the Area Under the
Receiver Operating Characteristic curve (AUC). Two scoring methods are
available: \code{"raw"} (raw expression, optionally truncated by
\code{min_expression}) and \code{"rank"} (expression ranks, robust to
dropout and outlier values). Optionally generates a publication-ready ROC
plot via \strong{ggplot2}.
}
\examples{
\dontrun{
# Raw expression with truncation
res_raw <- Compute_Gene_AUC_ROC(
  seurat_obj = sce, gene = "CD3D",
  group_col = "seurat_clusters", group_label = 1,
  method = "raw", min_expression = 0.5
)

# Rank-based (robust to dropout, no threshold needed)
res_rank <- Compute_Gene_AUC_ROC(
  seurat_obj = sce, gene = "CD3D",
  group_col = "seurat_clusters", group_label = 1,
  method = "rank"
)
print(res_rank$AUC)
}
}
\seealso{
Other Section_5_Other_Functions_Provided:
\code{\link[=Celltype_Compare]{Celltype_Compare()}},
\code{\link[=Celltype_annotation_Cellmarker2]{Celltype_annotation_Cellmarker2()}},
\code{\link[=Celltype_annotation_Excel]{Celltype_annotation_Excel()}},
\code{\link[=Celltype_annotation_PanglaoDB]{Celltype_annotation_PanglaoDB()}},
\code{\link[=Celltype_annotation_Seurat]{Celltype_annotation_Seurat()}}
}
\concept{Section_5_Other_Functions_Provided}
