% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plot_Group_Gene_Heatmap.R
\name{Plot_Group_Gene_Heatmap}
\alias{Plot_Group_Gene_Heatmap}
\title{Plot Grouped Gene Z-Score Heatmap with Optional Integrated Score Row}
\usage{
Plot_Group_Gene_Heatmap(
  seurat_obj,
  features,
  group_by,
  assay = NULL,
  slot_or_layer = "data",
  add_aucell_row = TRUE,
  aucell_row_name = "Integrated_Score",
  cluster_rows = FALSE,
  cluster_cols = FALSE,
  palette_set = "stallion",
  scale_range = c(-1.25, 1.25),
  annotation_legend = FALSE,
  cellwidth = 18,
  cellheight = 18,
  main = "Group Gene Heatmap Plot | SlimR"
)
}
\arguments{
\item{seurat_obj}{A Seurat object (supports both Seurat v4 and v5).}

\item{features}{A character vector of gene names to display in the heatmap.}

\item{group_by}{Character string specifying the column name in
\code{seurat_obj@meta.data} used to group cells (e.g., \code{"orig.ident"}
or \code{"celltype"}).}

\item{assay}{Character string specifying the assay to pull data from.
If \code{NULL}, uses \code{Seurat::DefaultAssay(seurat_obj)}.}

\item{slot_or_layer}{Character string specifying the layer/slot to extract.
Default is \code{"data"}.}

\item{add_aucell_row}{Logical. If \code{TRUE} (default), computes an integrated
mean expression signature across \code{features} per cell and appends its
group-level mean Z-score as the top row of the heatmap.}

\item{aucell_row_name}{Character string naming the top integrated row
when \code{add_aucell_row = TRUE}. Default is \code{"Integrated_Score"}.}

\item{cluster_rows}{Logical. Whether to perform hierarchical clustering on
rows. Default is \code{FALSE} to maintain input gene order.}

\item{cluster_cols}{Logical. Whether to perform hierarchical clustering on
columns. Default is \code{FALSE} to preserve original group ordering.}

\item{palette_set}{Character string naming the discrete palette set to use
for group/type annotation bars (e.g., \code{"stallion"}, \code{"bear"},
\code{"calm"}). Default is \code{"stallion"}.}

\item{scale_range}{Numeric vector of length 2 defining the minimum and
maximum limits for the heatmap break range. Default is \code{c(-1.25, 1.25)}.}

\item{annotation_legend}{Logical. Whether to show legends for the column and row
annotations (e.g., Group and Feature Type color bars). Default is \code{FALSE}.}

\item{cellwidth}{Numeric. Width of individual heatmap cells in points.
Set to make cells square. Default is \code{18}.}

\item{cellheight}{Numeric. Height of individual heatmap cells in points.
Set to make cells square. Default is \code{18}.}

\item{main}{Character string for the main plot title. Default is
\code{"Group Gene Heatmap Plot | SlimR"}.}
}
\value{
A \pkg{pheatmap} object invisibly.
}
\description{
Generates a publication-quality mean Z-score expression heatmap across cell
groups from a Seurat object using \pkg{pheatmap}. Optionally computes and
appends a top row representing the integrated pathway or marker signature
score (mean gene expression across target features per cell) before group
mean calculation and Z-score scaling. Row annotations distinguish individual gene
expression rows from the optional integrated weighted row. Column annotations
and row annotations are automatically styled using SlimR's internal discrete
palettes.
}
\section{Colour palette}{

Discrete annotation colors are generated by internal \code{paletteDiscrete()}
based on ArchR's palettes (\emph{stallion} by default). Heatmap body values use a
classic blue-white-red (\code{RdBu} reversed) palette matching custom breaks.
}

\examples{
\dontrun{
# Basic expression heatmap without integrated row
Plot_Group_Gene_Heatmap(
  seurat_obj = sce,
  features = c("CD3D", "CD3E", "CD4", "CD8A"),
  group_by = "celltype",
  add_aucell_row = FALSE
)

# With top integrated gene signature score row
Plot_Group_Gene_Heatmap(
  seurat_obj = sce,
  features = c("TNF", "IL6", "NFKB1", "RELA"),
  group_by = "orig.ident",
  add_aucell_row = TRUE,
  aucell_row_name = "NFkB_Pathway_AUC"
)
}
}
\seealso{
Other Section_5_Other_Functions_Provided:
\code{\link[=Celltype_Compare]{Celltype_Compare()}},
\code{\link[=Celltype_annotation_Cellmarker2]{Celltype_annotation_Cellmarker2()}},
\code{\link[=Celltype_annotation_Excel]{Celltype_annotation_Excel()}},
\code{\link[=Celltype_annotation_PanglaoDB]{Celltype_annotation_PanglaoDB()}},
\code{\link[=Celltype_annotation_Seurat]{Celltype_annotation_Seurat()}},
\code{\link[=Compute_Gene_AUC_ROC]{Compute_Gene_AUC_ROC()}},
\code{\link[=Plot_Hierarchy_Proportion]{Plot_Hierarchy_Proportion()}},
\code{\link[=Plot_Voronoi_diagram]{Plot_Voronoi_diagram()}},
\code{\link[=paletteDiscrete]{paletteDiscrete()}}
}
\concept{Section_5_Other_Functions_Provided}
