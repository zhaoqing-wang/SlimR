% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plot_Voronoi_Diagram.R
\name{Plot_Voronoi_diagram}
\alias{Plot_Voronoi_diagram}
\title{Generate a Weighted Voronoi Treemap for Hierarchical Cell Types}
\usage{
Plot_Voronoi_diagram(
  seurat_obj,
  Main_cell_types,
  Cell_types,
  label_type = c("both", "count", "percentage", "none"),
  shape = c("rounded_rect", "circle"),
  seed = 1,
  col_Cell_types = NULL,
  label_size = 3.5,
  label_color = "black",
  label_fontface = "bold",
  border_color = "grey90",
  subtype_border_lwd = 0.5,
  main_border_lwd = 1.5,
  outer_border_lwd = 2,
  legend = TRUE,
  legend_position = "right"
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{Main_cell_types}{Character string naming a column in
\code{seurat_obj@meta.data} that holds the top-level (main) cell-type labels.}

\item{Cell_types}{Character string naming a column in
\code{seurat_obj@meta.data} that holds the sub-type labels.}

\item{label_type}{Character string specifying the type of label to display
inside each polygon. One of \code{"both"}, \code{"count"}, \code{"percentage"},
or \code{"none"}. Default is \code{"both"}.}

\item{shape}{Character string specifying the shape of the polygons.
\code{"rounded_rect"} (default) or \code{"circle"}.}

\item{seed}{Integer seed for reproducibility of the Voronoi layout.
Default is \code{1}.}

\item{col_Cell_types}{Optional named or unnamed character vector of colours
for the \code{Cell_types} categories. If provided, it overrides the
default ArchR-derived palette.}

\item{label_size}{Numeric value controlling the size of the polygon labels.
Default is \code{3}.}

\item{label_color}{Colour of the polygon labels. Default is \code{"black"}.}

\item{label_fontface}{Font face for polygon labels (\code{"plain"},
\code{"italic"}, \code{"bold"}, etc.). Default is \code{"bold"}.}

\item{border_color}{Colour of the border lines separating polygons.
Default is \code{"grey90"} (a very light grey).}

\item{subtype_border_lwd}{Line width for borders between sub-type polygons.
Default is \code{0.15}.}

\item{main_border_lwd}{Line width for borders between main cell types.
Default is \code{0.35}.}

\item{outer_border_lwd}{Line width for the outer frame of the entire plot.
The frame is drawn as a tight convex hull around the outermost polygons,
perfectly following the natural outline of the treemap. Default is \code{0.4}.}

\item{legend}{Logical indicating whether to display a colour legend.
Default is \code{TRUE}.}

\item{legend_position}{Position of the legend when \code{legend = TRUE}.
Default is \code{"right"}.}
}
\value{
Invisibly, a list with \code{voronoi_treemap} (the raw treemap object) and
\code{plot} (the \pkg{ggplot2} object).
}
\description{
Creates a weighted Voronoi treemap displaying the hierarchical composition
of single-cell data. The area of each polygon is proportional to the
number of cells in the corresponding sub-type, and the polygons are
grouped by the main cell type. Each sub-type is filled with a distinct
colour from the \strong{ArchR}-inspired \emph{stallion} palette (or a user-supplied
custom palette).

\strong{Note on colour handling:}
The function uses \code{WeightedTreemaps::voronoiTreemap()} to compute polygon
coordinates, but \strong{replaces the original drawing routine} with a custom
\code{ggplot2}-based renderer. This is necessary because the upstream
\code{add_color()} function treats discrete palettes as continuous gradients
and does not respect the order of polygon cells, making it impossible
to guarantee exact colour correspondence with functions like
\code{Plot_Hierarchy_Proportion} or \code{Seurat::DimPlot}.
The custom drawing code extracts polygon vertices and uses
\code{scale_fill_manual()} with a named colour vector, ensuring that each
cell type receives exactly its intended colour regardless of internal
cell order.
}
\section{Colour palette}{

The default colour palette is derived from the \strong{ArchR} package
(\href{https://www.archrproject.com/}{ArchR project site},
\href{https://github.com/GreenleafLab/ArchR}{GitHub}).
If no custom palette is supplied, the function calls the internal
\code{paletteDiscrete()} function, which replicates the \emph{stallion} palette
from ArchR. When the number of categories exceeds the palette size,
colours are interpolated. ArchR is described in:
Granja JM, Corces MR et al. ArchR is a scalable software package for
integrative single-cell chromatin accessibility analysis.
\emph{Nature Genetics}, 2021. \doi{10.1038/s41588-021-00790-6}
}

\examples{
\dontrun{
# Basic rounded-rectangle treemap with both count and percentage labels
Plot_Voronoi_diagram(
  seurat_obj      = sce,
  Main_cell_types = "compartment",
  Cell_types      = "cell_type"
)

# Circular polygons, show only percentages, custom colours
Plot_Voronoi_diagram(
  sce,
  "compartment",
  "cell_type",
  label_type     = "percentage",
  shape          = "circle",
  seed           = 42,
  col_Cell_types = c(
    "Enterocyte"   = "#1f78b4",
    "Goblet"       = "#33a02c",
    "Stem"         = "#e31a1c",
    "Paneth"       = "#ff7f00"
  ),
  legend_position = "right"
)

# Only sub-type names, no legend
Plot_Voronoi_diagram(
  sce,
  "compartment",
  "cell_type",
  label_type = "none",
  legend     = FALSE
)
}
}
\seealso{
Other Section_5_Other_Functions_Provided:
\code{\link[=Celltype_Compare]{Celltype_Compare()}},
\code{\link[=Celltype_annotation_Cellmarker2]{Celltype_annotation_Cellmarker2()}},
\code{\link[=Celltype_annotation_Excel]{Celltype_annotation_Excel()}},
\code{\link[=Celltype_annotation_PanglaoDB]{Celltype_annotation_PanglaoDB()}},
\code{\link[=Celltype_annotation_Seurat]{Celltype_annotation_Seurat()}},
\code{\link[=Compute_Gene_AUC_ROC]{Compute_Gene_AUC_ROC()}},
\code{\link[=Plot_Hierarchy_Proportion]{Plot_Hierarchy_Proportion()}},
\code{\link[=paletteDiscrete]{paletteDiscrete()}}
}
\concept{Section_5_Other_Functions_Provided}
