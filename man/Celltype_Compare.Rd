% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Celltype_Compare.R
\name{Celltype_Compare}
\alias{Celltype_Compare}
\title{Compare cell type labels across two single-cell datasets after aligning cell barcodes}
\usage{
Celltype_Compare(
  sce_label,
  sce,
  label_col = NULL,
  group_col = NULL,
  barcode_col = NULL,
  color_low = "grey70",
  color_high = "navy",
  show_plot = TRUE
)
}
\arguments{
\item{sce_label}{A Seurat object containing the cell type label column.}

\item{sce}{A Seurat object containing the grouping column.}

\item{label_col}{Character. Name of the metadata column in \code{sce_label} that stores
cell type labels (e.g., "Sub_cell_type").}

\item{group_col}{Character. Name of the metadata column in \code{sce} that stores
grouping information (e.g., "SCT_snn_res.0.3").}

\item{barcode_col}{Optional character. Name of a metadata column in both objects
that contains the cell barcode identifiers. If \code{NULL}, the function uses
\code{colnames(sce_label)} and \code{colnames(sce)}.}

\item{color_low}{Character. Color for low proportion values in the heatmap.
Default: "grey70".}

\item{color_high}{Character. Color for high proportion values in the heatmap.
Default: "navy".}

\item{show_plot}{Logical. If \code{TRUE} (default), the heatmap is automatically
displayed when the function is called. Set to \code{FALSE} to suppress
automatic plotting (e.g., in non‑interactive environments).}
}
\value{
A list with five components:
\item{count_table}{A data frame (wide format) with rows = unique
\code{label_col} values and columns = unique \code{group_col} values; cell values are
raw counts of shared cells.}
\item{prop_table}{Same shape as \code{count_table}; each cell shows the proportion
of cells within a \code{group_col} column (column-wise sum = 1).}
\item{main_to_sub}{A data frame mapping each \code{group_col} value to the most
frequent \code{label_col} value among shared cells.}
\item{plot}{A ggplot2 heatmap object visualizing the proportion table.}
\item{match_info}{A tibble with columns \code{label_transform}, \code{sce_transform},
\code{shared_n} – the transformations used to align barcodes and the number of
shared cells after alignment.}
}
\description{
This function automatically aligns cell barcodes between two Seurat objects using a
variety of normalization transformations, then cross-tabulates a cell type label column
(from the first object) against a grouping column (from the second object). It returns
count tables, proportion tables, a dominant mapping, and a heatmap.
}
\details{
\strong{Cell barcode alignment}:
The function automatically tries a set of normalization functions on the cell
identifiers (either from \code{barcode_col} or from column names) to maximise the
number of shared barcodes between the two objects. Transformations include:
\code{identity}, \code{drop_numeric_suffix} (removes e.g., "-1-2"), \code{drop_suffix} (removes
"-1"), and several prefix removals. The transformation pair yielding the highest
number of shared identifiers is selected.

\strong{Proportion calculation}:
Proportions are computed \strong{within each \code{group_col} level} (column-wise),
i.e. for each group, the sum of proportions across all cell types equals 1.

\strong{Plot}:
The heatmap uses \code{ggplot2::geom_tile()} with a fixed coordinate ratio and a
colour gradient from \code{color_low} to \code{color_high}.
}
\examples{
\dontrun{
# Basic usage with two Seurat objects and default barcode alignment
result <- Celltype_Compare(
  sce_label = seurat_obj1,
  sce = seurat_obj2,
  label_col = "cell_type",
  group_col = "cluster"
)

# Access the proportion table
head(result$prop_table)

# View the dominant mapping
print(result$main_to_sub)

# Display the heatmap
print(result$plot)

# Use a custom barcode column
result2 <- Celltype_Compare(
  sce_label = seurat_obj1,
  sce = seurat_obj2,
  label_col = "cell_type",
  group_col = "cluster",
  barcode_col = "cell_barcode"
)
}

}
\seealso{
Other Section_5_Other_Functions_Provided:
\code{\link[=Celltype_annotation_Cellmarker2]{Celltype_annotation_Cellmarker2()}},
\code{\link[=Celltype_annotation_Excel]{Celltype_annotation_Excel()}},
\code{\link[=Celltype_annotation_PanglaoDB]{Celltype_annotation_PanglaoDB()}},
\code{\link[=Celltype_annotation_Seurat]{Celltype_annotation_Seurat()}},
\code{\link[=Compute_Gene_AUC_ROC]{Compute_Gene_AUC_ROC()}}
}
\concept{Section_5_Other_Functions_Provided}
