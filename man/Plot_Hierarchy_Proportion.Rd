% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plot_Hierarchy_Proportion.R
\name{Plot_Hierarchy_Proportion}
\alias{Plot_Hierarchy_Proportion}
\title{Plot a Hierarchical Cell Type Tree with Optional Group Proportion Heatmap}
\usage{
Plot_Hierarchy_Proportion(
  seurat_obj,
  Main_cell_types,
  col_Main_cell_types = NULL,
  Cell_types = NULL,
  col_Cell_types = NULL,
  Sub_cell_types = NULL,
  col_Sub_cell_types = NULL,
  proportion = TRUE,
  Groups = NULL,
  show_labels = TRUE,
  low_col = "white",
  high_col = "navy"
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{Main_cell_types}{Character string naming a column in
\code{seurat_obj@meta.data} that holds the top‑level cell‑type labels.}

\item{col_Main_cell_types}{Optional named or unnamed character vector of
colours for the \code{Main_cell_types} categories.  If \code{NULL},
colours are generated automatically.}

\item{Cell_types}{Character string naming a column with second‑level
(sub‑type) labels.  Use \code{NULL} if no second level exists.}

\item{col_Cell_types}{Optional colours for the \code{Cell_types} categories.}

\item{Sub_cell_types}{Character string naming a column with third‑level
(sub‑sub‑type) labels.  Use \code{NULL} if absent.  \code{Cell_types}
must be supplied when \code{Sub_cell_types} is used.}

\item{col_Sub_cell_types}{Optional colours for the \code{Sub_cell_types}
categories.}

\item{proportion}{Logical scalar.  If \code{TRUE} (the default), a
proportion heatmap is drawn below the tree; if \code{FALSE}, only the
tree with labels is returned.}

\item{Groups}{Character string naming a \code{meta.data} column that
defines sample groups for the proportion heatmap.  Required when
\code{proportion = TRUE}.}

\item{show_labels}{Logical scalar. If \code{TRUE} (the default), text
labels are placed next to the non‑leaf Main and Cell level nodes.}

\item{low_col}{Character string for the lowest proportion colour in the
heatmap. Default is \code{"white"}.}

\item{high_col}{Character string for the highest proportion colour in the
heatmap. Default is \code{"navy"}.}
}
\value{
Invisibly, a list with the following components:
\describe{
\item{tree_plot}{A \pkg{ggplot2} object with the hierarchical tree,
leaf sticks, and labels.}
\item{prop_plot}{If \code{proportion = TRUE}, a \pkg{ggplot2} heatmap with
top ticks; otherwise \code{NULL}.}
\item{combined_plot}{If \code{proportion = TRUE} and \pkg{patchwork} is
installed, a combined plot with title \emph{Hierarchical Proportion Plot | SlimR};
otherwise \code{NULL}.}
}
The function automatically prints the \code{combined_plot} (or the
\code{tree_plot} when no proportion is requested) to the active graphics
device.
}
\description{
Creates a publication‑quality composite figure with a hierarchical tree
panel and an optional proportion heatmap.  Leaf labels are connected to the
terminal nodes by short vertical sticks and placed at a fixed distance
below the tree, guaranteeing a clean separation and perfect top alignment
of all cell‑type names.  Short vertical ticks on top of the heatmap mark
each column, aligning with the labels above.  The tree height is independent
of label length; the heatmap sits tightly beneath the labels.
}
\section{Colour palette}{

The default colour palette is derived from the \strong{ArchR} package
(\href{https://www.archrproject.com/}{ArchR project site},
\href{https://github.com/GreenleafLab/ArchR}{GitHub}).
If no custom palette is supplied, the function calls the internal
\code{paletteDiscrete()} function, which replicates the \emph{stallion} palette
from ArchR. When the number of categories exceeds the palette size,
colours are interpolated. ArchR is described in:
Granja JM, Corces MR et al. ArchR is a scalable software package for
integrative single-cell chromatin accessibility analysis.
\emph{Nature Genetics}, 2021. \doi{10.1038/s41588-021-00790-6}
}

\examples{
\dontrun{
# Full three-level hierarchy with proportion heatmap
res <- Plot_Hierarchy_Proportion(
  seurat_obj          = sce,
  Main_cell_types     = "Main_cell_type",
  Cell_types          = "Cell_type",
  Sub_cell_types      = "Sub_cell_type",
  proportion          = TRUE,
  Groups              = "orig.ident"
)

# Only the tree, user-specified colours
res <- Plot_Hierarchy_Proportion(
  sce,
  "Main_cell_type",
  col_Main_cell_types = c(Immune = "red", Stromal = "blue", Epithelial = "green"),
  "Cell_type",
  proportion = FALSE
)
}
}
\seealso{
Other Section_5_Other_Functions_Provided:
\code{\link[=Celltype_Compare]{Celltype_Compare()}},
\code{\link[=Celltype_annotation_Cellmarker2]{Celltype_annotation_Cellmarker2()}},
\code{\link[=Celltype_annotation_Excel]{Celltype_annotation_Excel()}},
\code{\link[=Celltype_annotation_PanglaoDB]{Celltype_annotation_PanglaoDB()}},
\code{\link[=Celltype_annotation_Seurat]{Celltype_annotation_Seurat()}},
\code{\link[=Compute_Gene_AUC_ROC]{Compute_Gene_AUC_ROC()}},
\code{\link[=Plot_Voronoi_diagram]{Plot_Voronoi_diagram()}},
\code{\link[=paletteDiscrete]{paletteDiscrete()}}
}
\concept{Section_5_Other_Functions_Provided}
